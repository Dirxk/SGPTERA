@{
    ViewData["Title"] = "Clientes";
    var IdUsuario = ViewBag.IdUsuario;

}

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Rubik:wght@500&display=swap" rel="stylesheet">

<!-- ================================================================================================= -->
<!-- ======================================== Codigo Principal ======================================= -->
<!-- ================================================================================================= -->
<div class="card d-flex flex-column">
    <div class="card-header d-flex flex-column flex-md-row align-items-center justify-content-center justify-content-md-between">
        <!-- Primera columna: Título -->
        <div class="col-12 col-md-6 d-flex align-items-center justify-content-center justify-content-md-start mb-3 mb-md-0">
            <i class="fa-solid fa-users fa-lg me-2 d-none d-md-block"></i>
            <i class="fa-solid fa-users me-2 mb-2 d-block d-md-none"></i>
            <h3 class="m-0 d-none d-md-block" style="font-family: 'Rubik', sans-serif; font-size: 1.25rem;">Clientes</h3>
            <h6 class="m-0 d-block d-md-none mb-1" style="font-family: 'Rubik', sans-serif; font-size: 1rem;">Clientes</h6>
        </div>

        <!-- Segunda columna: Búsqueda y botones -->
        <div class="col-12 col-md-6 d-flex justify-content-center justify-content-md-end gap-2 mt-2 mt-md-0">
            <!-- Input de búsqueda -->
            <input id="inputBusqueda" class="form-control form-control-sm w-50" type="search" placeholder="Buscar" aria-label="Search">
            <!-- Botón de búsqueda -->
            <button id="btnBuscar" class="btn btn-info btn-sm" type="submit">
                <i class="fas fa-search"></i>
            </button>
            <!-- Botón de recargar -->
            <button type="button" id="btnReloadClientes" name="btnReloadClientes" class="btn btn-warning btn-sm">
                <i class="fas fa-sync-alt"></i>
            </button>
            <!-- Botón de agregar cliente -->
            <button type="button" class="btn btn-success btn-sm" data-bs-toggle="modal" data-bs-target="#UserModal">
                <i class="fas fa-plus"></i>
            </button>
        </div>
    </div>
    <div class="card-body p-3 flex-grow-1">
        <div class="table-responsive scrollbar h-100">
            <!-- Grilla de clientes -->
            <div id="GridClientes" style="width: 100%; height: calc(75vh - 50px);"></div>
            <!-- Mensaje de "Cliente no encontrado" -->
            <div id="mensajeNoEncontrado" class="text-danger mt-2" style="display: none;">Cliente no encontrado</div>
        </div>
    </div>
</div>

<!-- ================================================================================================= -->
<!-- ======================================= Modal nuevo cliente ===================================== -->
<!-- ================================================================================================= -->

<div class="modal fade" id="UserModal" tabindex="-1" aria-labelledby="UserModalLabel" aria-hidden="true" data-bs-backdrop="static" data-bs-keyboard="false">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">
            <form id="FormCrearProyecto">
                <div class="modal-header">
                    <h3 class="modal-title" id="UserModalLabel">Agregar nuevo cliente</h3>
                    <button type="button" class="btn-close cerrarModal" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <!-- Campo: Razón Social -->
                        <div class="col-12 col-md-6 mb-3">
                            <div class="form-floating">
                                <input class="form-control" id="InpRazonSocial" name="InpRazonSocial" type="text" placeholder="Razón Social" required autocomplete="off" />
                                <label for="InpRazonSocial">Razón Social <span class="text-danger">*</span></label>
                            </div>
                        </div>
                        <!-- Campo: RFC -->
                        <div class="col-12 col-md-6 mb-3">
                            <div class="form-floating">
                                <input class="form-control text-uppercase" id="InpRFC" name="InpRFC" type="text" placeholder="RFC" maxlength="13" required autocomplete="off" />
                                <label for="InpRFC">RFC <span class="text-danger">*</span></label>
                            </div>
                        </div>
                        <!-- Campo: Prefijo -->
                        <div class="col-12 col-md-6 mb-3">
                            <div class="form-floating">
                                <input class="form-control text-uppercase" id="InpPrefijo" name="InpPrefijo" type="text" placeholder="Prefijo" maxlength="15" required autocomplete="off" />
                                <label for="InpPrefijo">Prefijo <span class="text-danger">*</span></label>
                            </div>
                        </div>
                        <!-- Campo: Teléfono -->
                        <div class="col-12 col-md-6 mb-3">
                            <div class="form-floating">
                                <input class="form-control" id="InpTelefono" name="InpTelefono" type="text" placeholder="Teléfono" maxlength="15" required autocomplete="off" />
                                <label for="InpTelefono">Teléfono <span class="text-danger">*</span></label>
                            </div>
                        </div>
                        <!-- Campo: Imagen -->
                        <div class="col-12 mb-3">
                            <label class="fw-bold mb-2" style="font-size: 1rem;">Imagen para el usuario:</label>
                            <input class="form-control" id="InpImagenExt" name="imagen" type="file" accept="image/*"/>
                        </div>
                        <!-- Previsualización de la imagen -->
                        <div class="col-12 mb-3">
                            <div id="previewContainer" class="d-none border rounded p-3 d-flex align-items-center justify-content-between">
                                <div class="d-flex align-items-center">
                                    <img id="previewImage" src="" alt="Previsualización" class="img-thumbnail me-3" style="max-width: 150px; max-height: 150px; object-fit: contain;">
                                    <div>
                                        <p id="fileName" class="mb-0 fw-bold"></p>
                                        <small id="fileSize" class="text-muted"></small>
                                    </div>
                                </div>
                                <button type="button" class="btn btn-link text-danger p-0" id="removePreview" aria-label="Eliminar previsualización">
                                    <i class="fas fa-trash-alt"></i>
                                </button>
                            </div>
                        </div>
                        <!-- Contenedor para alertas -->
                        <div class="col-12">
                            <div class="ContAlerta"></div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-danger cerrarModal" data-bs-dismiss="modal">
                        <i class="fas fa-ban me-2"></i>Cancelar
                    </button>
                    <button type="submit" class="btn btn-success px-5" id="BtnAgregarUsuarioExt">
                        <i class="fas fa-user-plus me-2"></i>Agregar cliente
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- ================================================================================================= -->
<!-- ====================================== Modal editar cliente ===================================== -->
<!-- ================================================================================================= -->

<div class="modal fade" id="ModalEditarCliente" tabindex="-1" aria-labelledby="ModalEditarClienteLabel" aria-hidden="true" data-bs-backdrop="static" data-bs-keyboard="false">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">
            <form id="FormEditarCliente">
                <div class="modal-header">
                    <h5 class="modal-title" id="ModalEditarClienteLabel">Editar Cliente</h5>
                    <button type="button" class="btn-close cerrarModalEditarCliente" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <!-- Campo: Razon social -->
                        <div class="col-md-6 mb-3">
                            <div class="form-floating">
                                <input class="form-control" id="InpRazonSocialEdit" name="InpRazonSocialEdit" type="text" placeholder="Razón Social" required autocomplete="off" />
                                <label for="InpRazonSocialEdit">Razón Social</label>
                            </div>
                        </div>
                        <!-- Campo: RFC -->
                        <div class="col-md-6 mb-3">
                            <div class="form-floating">
                                <input class="form-control text-uppercase" id="InpRFCEdit" name="InpRazonSocialEdit" type="text" placeholder="RFC" maxlength="13" required autocomplete="off" />
                                <label for="InpRFCEdit">RFC</label>
                            </div>
                        </div>
                        <!-- Campo: Prefijo -->
                        <div class="col-md-6 mb-3">
                            <div class="form-floating">
                                <input class="form-control text-uppercase" id="InpPrefijoEdit" name="InpRazonSocialEdit" type="text" placeholder="Prefijo" maxlength="15" required autocomplete="off" />
                                <label for="InpPrefijoEdit">Prefijo</label>
                            </div>
                        </div>
                        <!-- Campo: Telefono -->
                        <div class="col-md-6 mb-3">
                            <div class="form-floating">
                                <input class="form-control" id="InpTelefonoEdit" type="text" placeholder="Telefono" maxlength="15" required autocomplete="off" />
                                <label for="InpPrefijoEdit">Telefono</label>
                            </div>
                        </div>
                        <!-- Campo: Imagen -->
                        <div class="col-sm-12 mt-4">
                            <label class="fw-bold mb-2" style="font-size: 1rem;">Logotipo del cliente:</label>
                            <input class="form-control" id="InpClienteImagenEdit" name="imagen" type="file" accept="image/*" />
                        </div>
                        <div class="col-12 mt-3">
                            <div id="previewContainerEdit" class="border rounded p-3 d-flex align-items-center justify-content-between">
                                <div class="d-flex align-items-center">
                                    <img id="previewImageEdit" src="" alt="Previsualización" class="img-thumbnail me-3 previewImage" style="max-width: 150px; max-height: 150px; object-fit: contain;">
                                    <div>
                                        <p id="fileNameEdit" class="mb-0 fw-bold"></p>
                                        <small id="fileSizeEdit" class="text-muted"></small>
                                    </div>
                                </div>
                                
                            </div>
                        </div>
                        <div id="ContAlertaEdit" class="col-12"></div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-danger cerrarModalEditarCliente">Cancelar</button>
                    <button type="button" class="btn btn-success EditarCliente" id="btnActualizarCliente">Actualizar Cliente</button>
                </div>
            </form> 
        </div>
    </div>
</div>

<!-- ================================================================================================= -->
<!-- ====================================== Modal eliminar cliente =================================== -->
<!-- ================================================================================================= -->

<!-- Modal de Eliminación -->
<div class="modal fade" data-bs-backdrop="static" data-bs-keyboard="false" id="ModalEliminarCliente" tabindex="-1" aria-labelledby="verticallyCenteredModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form id="FormEliminarCliente">
                <div class="modal-header">
                    <h5 class="modal-title" id="verticallyCenteredModalLabel">Eliminar Cliente</h5>
                    <button class="btn p-1" type="button" data-bs-dismiss="modal" aria-label="Close">
                        <span class="fas fa-times fs--1"></span>
                    </button>
                </div>
                <div class="modal-body">
                    <p class="text-700 lh-lg mb-0">
                        ¿Deseas eliminar el cliente <strong class="nombreCliente"></strong>?
                    </p>
                </div>
                <div class="modal-footer">
                    <div class="col-auto">
                        <button class="btn btn-primary me-1 mb-1" type="button" data-bs-dismiss="modal">Cancelar</button>
                    </div>
                    <div class="col-auto">
                        <button class="btn btn-danger me-1 mb-1" type="submit" id="BtnEliminarCliente">Eliminar</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- ================================================================================================= -->
<!-- ====================================== Modal reactivar cliente ================================== -->
<!-- ================================================================================================= -->

<div class="modal fade" data-bs-backdrop="static" data-bs-keyboard="false" id="ModalConfirmarCliente" tabindex="-1" aria-labelledby="verticallyCenteredModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form id="FormConfirmarCliente">
                <div class="modal-header">
                    <h5 class="modal-title" id="verticallyCenteredModalLabel">Reactivar Cliente</h5>
                    <button class="btn p-1" type="button" data-bs-dismiss="modal" aria-label="Close">
                        <span class="fas fa-times fs--1"></span>
                    </button>
                </div>
                <div class="modal-body">
                    <p class="text-700 lh-lg mb-0">
                        ¿Deseas reactivar el cliente <strong class="nombreConfirmarCliente"></strong>?
                    </p>
                </div>
                <div class="modal-footer">
                    <div class="col-auto">
                        <button class="btn btn-primary me-1 mb-1" type="button" data-bs-dismiss="modal">Cancelar</button>
                    </div>
                    <div class="col-auto">
                        <button class="btn btn-success me-1 mb-1" type="submit" id="BtnConfirmarCliente">Reactivar</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>


@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.5/jquery.validate.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>

        //============================================================================================================================\\
        //====================================================  Variables Globales  ===================================================\\
        //==============================================================================================================================\\
        // Variable global para almacenar la instancia de la grilla de clientes.
        var gridClientes = null;

        // Función que realiza una solicitud AJAX para obtener los clientes desde el servidor.
        function verClientes() {
            return new Promise((resolve, reject) => {
                const startTime = performance.now(); // Inicia el contador para medir el tiempo de respuesta.

                $.ajax({
                    type: 'POST', // Método HTTP utilizado para la solicitud.
                    data: {}, // Datos enviados al servidor (en este caso, vacío).
                    url: '@Url.Action("GetClientes", "Catalogos")',
                    success: function (datos) {
                        const endTime = performance.now(); // Finaliza el contador de tiempo.
                        const serverResponseTime = endTime - startTime; // Calcula el tiempo de respuesta del servidor.
                        gridClientes.itemsSource = datos.data; // Asigna los datos recibidos a la grilla de clientes.
                        resolve(serverResponseTime); // Resuelve la promesa con el tiempo de respuesta.
                    },
                    error: function () {
                        console.error("Error al obtener los clientes."); // Maneja errores en la solicitud.
                        reject(); // Rechaza la promesa en caso de error.
                    }
                });
            });
        }

        //============================================================================================================================\\
        //================================  Función para editar un cliente específico basado en su ID  ================================\\
        //==============================================================================================================================\\

        let valoresIniciales = {}; // Almacena los valores iniciales del cliente
        const camposEdicion = ['#InpRazonSocialEdit', '#InpRFCEdit', '#InpPrefijoEdit', '#InpTelefonoEdit', '#InpClienteImagenEdit'];

        function EditarCliente(id) {
            const cliente = gridClientes.itemsSource.find(c => c.id === id); // Busca el cliente en la grilla.
            if (cliente) {
                // Guardar los valores iniciales del cliente
                valoresIniciales = {
                    razonSocial: cliente.razonSocial || '',
                    rfc: cliente.rfc || '',
                    prefijo: cliente.prefijo || '',
                    telefono: cliente.telefono || '',
                    logotipo: cliente.logotipo || ''
                };

                // Asigna los valores del cliente a los campos del formulario de edición.
                $('#InpRazonSocialEdit').val(valoresIniciales.razonSocial);
                $('#InpRFCEdit').val(valoresIniciales.rfc);
                $('#InpPrefijoEdit').val(valoresIniciales.prefijo);
                $('#InpTelefonoEdit').val(valoresIniciales.telefono);

                // Muestra la imagen del cliente si está disponible, de lo contrario, muestra una imagen por defecto.
                if (cliente.logotipo && typeof cliente.logotipo === 'string') {
                    $('#previewImageEdit').attr('src', `data:image/png;base64,${cliente.logotipo}`);
                    $('#previewContainerEdit').css('display', 'flex');
                } else {
                    // Ruta de la imagen por defecto (relativa desde la raíz del sitio)
                    const rutaImagenPorDefecto = "/lib/dashboard/assets/img/IMAGE.svg";

                    // Asigna la imagen por defecto al atributo src de la imagen
                    $('#previewImageEdit').attr('src', rutaImagenPorDefecto);
                    $('#previewContainerEdit').css('display', 'flex');
                }

                // Deshabilitar el botón de actualizar inicialmente
                $('#btnActualizarCliente').prop('disabled', true);

                // Muestra el modal de edición de cliente.
                const modalEditarCliente = new bootstrap.Modal($('#ModalEditarCliente')[0]);
                modalEditarCliente.show();
                $('#btnActualizarCliente').data('id', id); // Asigna el ID del cliente al botón de actualizar.
            } else {
                console.error("Cliente no encontrado."); // Maneja el caso en que el cliente no existe.
            }
        }

        // Función para habilitar el botón si algún campo ha cambiado
        function habilitarBotonActualizar() {
            let algunCampoCambiado = false;

            // Comparar los valores actuales con los valores iniciales
            if ($('#InpRazonSocialEdit').val() !== valoresIniciales.razonSocial ||
                $('#InpRFCEdit').val() !== valoresIniciales.rfc ||
                $('#InpPrefijoEdit').val() !== valoresIniciales.prefijo ||
                $('#InpTelefonoEdit').val() !== valoresIniciales.telefono ||
                $('#InpClienteImagenEdit')[0].files.length > 0) {
                algunCampoCambiado = true;
            }

            // Habilita el botón si algún campo ha cambiado
            $('#btnActualizarCliente').prop('disabled', !algunCampoCambiado);
        }

        // Escucha los cambios en los campos del formulario
        camposEdicion.forEach(function (campo) {
            $(campo).on('input change', function () {
                habilitarBotonActualizar();
            });
        });

        // Escucha el evento de cambio en el input de la imagen
        $('#InpClienteImagenEdit').on('change', function (event) {
            const file = event.target.files[0]; // Obtiene el archivo seleccionado.
            if (file) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    $('#previewImageEdit').attr('src', e.target.result); // Muestra la imagen.
                    $('#fileNameEdit').text(file.name); // Muestra el nombre del archivo.
                    $('#fileSizeEdit').text((file.size / 1024).toFixed(2) + ' KB'); // Muestra el tamaño del archivo.
                    $('#previewContainerEdit').removeClass('d-none'); // Muestra el contenedor de previsualización.
                };
                reader.readAsDataURL(file); // Lee el archivo como URL.
            }
            habilitarBotonActualizar();
        });

        

        // Evento para limpiar el formulario al cerrar el modal
        $('#ModalEditarCliente').on('hidden.bs.modal', function () {
            $('#FormEditarCliente')[0].reset(); // Restablece el formulario.
            $('#previewImageEdit').attr('src', ''); // Limpia la imagen.
            $('#fileNameEdit').text(''); // Limpia el nombre del archivo.
            $('#fileSizeEdit').text(''); // Limpia el tamaño del archivo.
            $('#btnActualizarCliente').prop('disabled', true); // Bloquea el botón de actualizar.
        });

        //============================================================================================================================\\
        //===============================  Inicialización del GRID Clientes y Configuración de Eventos  ===============================\\
        //==============================================================================================================================\\
 
        $(document).ready(function () {

            //============================================================================================================================\\
            //==========================================               GRID Principal                ======================================\\
            //==============================================================================================================================\\
            gridClientes = new wijmo.grid.FlexGrid('#GridClientes', {
                autoGenerateColumns: false,
                headersVisibility: 'Column',
                itemsSource: [],
                selectionMode: wijmo.grid.SelectionMode.Row,
                columns: [
                    { binding: '', header: '#', width: 50, align: 'center' },
                    { binding: 'razonSocial', header: 'Razón Social', minWidth: 350, width: '*', align: 'center' },
                    { binding: 'rfc', header: 'RFC', width: 200, align: 'center' },
                    { binding: 'prefijo', header: 'Prefijo', width: 150, align: 'center' },
                    { binding: 'telefono', header: 'Telefono', width: 200, align: 'center' },
                    { header: '', isReadOnly: true, width: 80, align: 'center' }
                ],
                isReadOnly: true,
                itemFormatter: function (panel, r, c, cell) {
                    if (panel.cellType === wijmo.grid.CellType.Cell) {
                        const rowData = panel.rows[r].dataItem; // Obtiene los datos de la fila.
                        const flgActivo = rowData.flgActivo; // Verificar el valor de FlgActivo.

                        // Cambiar el color de fondo de la fila si flgActivo es false
                        // if (flgActivo === false) {
                        //     cell.style.backgroundColor = '#BE3144'; // Color rojo
                        //     cell.style.color = '#FFFFFF'; // Texto blanco para mejor contraste
                        // }

                        if (c === 0) {
                            cell.textContent = (r + 1).toString(); // Muestra el número de fila.
                        } else if (c === panel.columns.length - 1) {
                            // Crear los botones según el valor de FlgActivo
                            let botones = '';
                            if (flgActivo === true) {
                                botones = `
                            <div class="d-flex justify-content-center align-items-center gap-2">
                                <button style="height: 25px; width: 25px;" class="btn btn-phoenix-secondary btn-icon fs--2 text-900 abrirModalEditarCliente" onclick="EditarCliente(${rowData.id})">
                                    <i class="fas fa-edit"></i>
                                </button>
                                <button style="height: 25px; width: 25px;" class="btn btn-phoenix-secondary btn-icon fs--2 text-danger abrirModalEliminarCliente" data-id="${rowData.id}" data-nombre="${rowData.razonSocial}">
                                    <i class="fas fa-trash-alt"></i>
                                </button>
                            </div>`;
                            } else if (flgActivo === false) {
                                botones = `
                            <div class="d-flex justify-content-center align-items-center gap-2">
                                <button style="height: 25px; width: 25px;" class="btn btn-phoenix-secondary btn-icon fs--2 text-success abrirModalConfirmarCliente" data-id="${rowData.id}" data-nombre="${rowData.razonSocial}">
                                    <i class="fas fa-check"></i>
                                </button>
                            </div>`;
                            }

                            // Insertar los botones en la celda
                            cell.innerHTML = botones;
                        }
                    }
                }
            });


            verClientes();

            //============================================================================================================================\\
            //==========================================  Funcionalidad de Búsqueda y Autocompletar   =====================================\\
            //==============================================================================================================================\\
            $('#inputBusqueda').autocomplete({
                source: function (request, response) {
                    const term = request.term.toLowerCase(); // Obtiene el término de búsqueda.
                    const clientes = gridClientes.itemsSource; // Obtiene la lista de clientes.
                    const resultados = clientes.filter(cliente =>
                        cliente.razonSocial.toLowerCase().includes(term) || // Busca por razón social.
                        cliente.rfc.toLowerCase().includes(term) || // Busca por RFC.
                        cliente.prefijo.toLowerCase().includes(term) || // Busca por prefijo.
                        cliente.telefono.toLowerCase().includes(term) // Busca por teléfono.
                    );
                    response(resultados.map(cliente => ({
                        label: `${cliente.razonSocial} (${cliente.rfc})`, // Muestra la razón social y RFC.
                        value: cliente.razonSocial // Asigna el ID del cliente como valor.
                    })));
                },
                select: function (event, ui) {
                    const clienteId = ui.item.value; // Obtiene el ID del cliente seleccionado.
                    const cliente = gridClientes.itemsSource.find(c => c.id === clienteId); // Busca el cliente en la grilla.
                    if (cliente) {
                        gridClientes.itemsSource = [cliente]; // Filtra la grilla para mostrar solo el cliente seleccionado.
                    }
                }
            });

            // Evento para buscar al hacer clic en el botón de búsqueda.
            $('#btnBuscar').on('click', function () {
                const term = $('#inputBusqueda').val().toLowerCase(); // Obtiene el término de búsqueda.
                const clientes = gridClientes.itemsSource; // Obtiene la lista de clientes.
                const resultados = clientes.filter(cliente =>
                    cliente.razonSocial.toLowerCase().includes(term) || // Busca por razón social.
                    cliente.rfc.toLowerCase().includes(term) || // Busca por RFC.
                    cliente.prefijo.toLowerCase().includes(term) || // Busca por prefijo.
                    cliente.telefono.toLowerCase().includes(term) // Busca por teléfono.
                );

                if (resultados.length > 0) {
                    gridClientes.itemsSource = resultados; // Filtra la grilla para mostrar los resultados.
                    $('#mensajeNoEncontrado').hide(); // Oculta el mensaje de "Puesto no encontrado".
                } else {
                    Swal.fire({
                        icon: 'error',
                        title: 'Oops...',
                        text: 'No se encontró ningún resultado',
                    });
                }
            });

            // Evento para limpiar la búsqueda y mostrar todos los clientes.
            $('#inputBusqueda').on('input', function () {
                if ($(this).val() === '') {
                    verClientes(); // Recarga todos los clientes.
                    $('#mensajeNoEncontrado').hide(); // Oculta el mensaje de "Cliente no encontrado".
                }
            });

            //============================================================================================================================\\
            //==========================================      Funcionalidad del botón Recargar       ======================================\\
            //==============================================================================================================================\\

            $('#btnReloadClientes').on('click', function () {
                // Ocultar el grid de clientes
                document.getElementById('GridClientes').style.visibility = 'hidden';

                // Bloquear el contenido principal con el estilo "arrows"
                Notiflix.Block.arrows('.card', 'Actualizando datos...', {
                    backgroundColor: 'rgba(255, 255, 255, 0.8)', // Fondo semitransparente
                    svgSize: '50px', // Tamaño del ícono de flechas
                    messageFontSize: '16px', // Tamaño del mensaje
                });

                // Llamar a la función verClientes para recargar el grid
                verClientes().then((serverResponseTime) => {
                    // Tiempo adicional de 500 ms después de la respuesta del servidor
                    setTimeout(() => {
                        // Desbloquear el contenido principal
                        Notiflix.Block.remove('.card');

                        // Mostrar el grid después de que termine la animación de éxito
                        document.getElementById('GridClientes').style.visibility = 'visible';
                    }, serverResponseTime + 500); // Tiempo del servidor + 500 ms
                }).catch(() => {
                    // En caso de error, desbloquear el contenido principal
                    Notiflix.Block.remove('.card');

                    // Mostrar un mensaje de error con SweetAlert2 (puedes cambiarlo por Notiflix si prefieres)
                    Swal.fire({
                        title: 'Error',
                        text: 'Hubo un error al recargar los clientes.',
                        icon: 'error',
                        timer: 2000,
                        timerProgressBar: true,
                        showConfirmButton: false,
                        allowOutsideClick: false,
                        allowEscapeKey: false,
                        allowEnterKey: false
                    });

                    // Mostrar el grid incluso si hay un error
                    document.getElementById('GridClientes').style.visibility = 'visible';
                });
            });
      
            //============================================================================================================================\\
            //========================================== Funcionalidad del boton de Agregar Cliente  ======================================\\
            //==============================================================================================================================\\

            $("#InpTelefono").on("input", function () {
                $(this).val($(this).val().replace(/[^0-9+ ]/g, ""));
            });
            // Validación del formulario
            $("#FormCrearProyecto").validate({
                rules: {
                    InpRazonSocial: { required: true, minlength: 2 },
                    InpRFC: { required: true, minlength: 12, maxlength: 13 },
                    InpPrefijo: { required: true },
                    InpTelefono: { required: true, maxlength: 15 }
                },
                messages: {
                    InpRazonSocial: { required: "La razón social es requerida.", minlength: "Mínimo 2 caracteres." },
                    InpRFC: { required: "El RFC es requerido.", minlength: "El RFC debe tener al menos 12 caracteres.", maxlength: "El RFC no puede tener más de 13 caracteres." },
                    InpPrefijo: { required: "El Prefijo es requerido." },
                    InpTelefono: { required: "El Telefono es requerido.", maxlength: "El telefono no puede tener más de 15 caracteres." }
                },
                submitHandler: function (form, event) {
                    event.preventDefault();
                    agregarCliente(); // Llamar a la función sin pasar IdUsuario
                },
                errorPlacement: function (error, element) {
                    error.addClass('col-12 mt-2 bg-danger text-white text-center p-2 rounded');
                    error.insertAfter(element);
                },
                errorElement: 'div',
                errorClass: 'alert-validate'
            });

            // Evento para previsualizar una imagen seleccionada
            $('#InpImagenExt').on('change', function (event) {
                const file = event.target.files[0]; // Obtiene el archivo seleccionado.
                if (file) {
                    const reader = new FileReader();
                    reader.onload = function (e) {
                        $('#previewImage').attr('src', e.target.result); // Muestra la imagen.
                        $('#fileName').text(file.name); // Muestra el nombre del archivo.
                        $('#fileSize').text((file.size / 1024).toFixed(2) + ' KB'); // Muestra el tamaño del archivo.
                        $('#previewContainer').removeClass('d-none'); // Muestra el contenedor de previsualización.
                    };
                    reader.readAsDataURL(file); // Lee el archivo como URL.
                }
            });

            // Evento para eliminar la previsualización de la imagen
            $('#removePreview').on('click', function () {
                $('#previewImage').attr('src', ''); // Limpia la imagen.
                $('#fileName').text(''); // Limpia el nombre del archivo.
                $('#fileSize').text(''); // Limpia el tamaño del archivo.
                $('#previewContainer').addClass('d-none'); // Oculta el contenedor de previsualización.
                $('#InpImagenExt').val(''); // Limpia el input de archivo.
            });

            // Evento para limpiar el formulario al cerrar el modal
            $('#UserModal').on('hidden.bs.modal', function () {
                $('#FormCrearProyecto')[0].reset(); // Restablece el formulario.
                $('#FormCrearProyecto').validate().resetForm();
                $('#previewImage').attr('src', ''); // Limpia la imagen.
                $('#fileName').text(''); // Limpia el nombre del archivo.
                $('#fileSize').text(''); // Limpia el tamaño del archivo.
                $('#previewContainer').addClass('d-none'); // Oculta el contenedor de previsualización.
            });


            // Función para enviar los datos del formulario mediante AJAX
            function agregarCliente() {
                var formData = new FormData();
                formData.append('RazonSocial', $('#InpRazonSocial').val());
                formData.append('RFC', $('#InpRFC').val());
                formData.append('Prefijo', $('#InpPrefijo').val());
                formData.append('Telefono', $('#InpTelefono').val());
                formData.append('LogotipoFile', $('#InpImagenExt')[0].files[0]); // Cambiado a LogotipoFile para coincidir con el backend

                $.ajax({
                    type: "POST",
                    url: '@Url.Action("AgregarCliente", "Catalogos")',
                    data: formData,
                    processData: false,
                    contentType: false,
                    beforeSend: function () {
                        // Aquí no se oculta el grid ni se muestra el bloqueo
                    },
                    success: function (response) {
                        if (response.resultado) {
                            // Ocultar el grid de puestos
                            document.getElementById('GridClientes').style.visibility = 'hidden';

                            // Bloquear el contenido principal con el estilo "hourglass"
                            Notiflix.Block.circle('.card', 'Agregando Cliente...', {
                                backgroundColor: 'rgba(255, 255, 255, 0.8)', // Fondo semitransparente
                                svgSize: '50px', // Tamaño del ícono de reloj de arena
                                messageFontSize: '16px', // Tamaño del mensaje
                            });

                            // Llamar a la función verPuestos para recargar el grid
                            verClientes().then((serverResponseTime) => {
                                // Tiempo adicional de 500 ms después de la respuesta del servidor
                                setTimeout(() => {
                                    // Desbloquear el contenido principal
                                    Notiflix.Block.remove('.card');

                                    // Mostrar el grid después de que termine la animación de éxito
                                    document.getElementById('GridClientes').style.visibility = 'visible';
                                }, serverResponseTime + 500); // Tiempo del servidor + 500 ms
                            }).catch(() => {
                                // En caso de error, desbloquear el contenido principal
                                Notiflix.Block.remove('.card');
                                document.getElementById('GridClientes').style.visibility = 'visible';
                                // Mostrar un mensaje de error con SweetAlert2
                                Swal.fire({
                                    title: 'Error',
                                    text: 'Hubo un error al recargar los puestos.',
                                    icon: 'error',
                                    timer: 2000,
                                    showConfirmButton: true,
                                    allowOutsideClick: false,
                                    allowEscapeKey: false,
                                    allowEnterKey: false
                                });

                                // Mostrar el grid incluso si hay un error
                                document.getElementById('GridClientes').style.visibility = 'visible';
                            });
                        } else {
                            // Mostrar un mensaje de error con SweetAlert2 si la respuesta no es exitosa
                            Swal.fire({
                                icon: "error",
                                title: "Error",
                                text: response.mensaje,
                                showConfirmButton: true,
                                allowOutsideClick: false,
                                allowEscapeKey: false,
                                allowEnterKey: false

                            });
                        }
                    },
                    error: function (jqXHR, textStatus, errorThrown) {
                        // Mostrar un mensaje de error con SweetAlert2 en caso de error en la solicitud
                        Swal.fire({
                            icon: "error",
                            title: "Error",
                            text: "Ocurrió un error al agregar el puesto.",
                            showConfirmButton: true,
                            allowOutsideClick: false,
                            allowEscapeKey: false,
                            allowEnterKey: false
                        });

                        console.error("Error en la solicitud:", textStatus, errorThrown);
                    },
                    complete: function () {
                        // Desbloquear el contenido principal después de 500 ms adicionales
                        setTimeout(() => {
                            Notiflix.Block.remove('.card');
                        }, 500);
                    }
                });
                
                const UserModal = bootstrap.Modal.getInstance(document.getElementById('UserModal'));
                UserModal.hide(); 
            }

            //============================================================================================================================\\
            //==========================================   Funcionalidad del botón Editar Cliente    ======================================\\
            //==============================================================================================================================\\
           
            $('.cerrarModalEditarCliente').on('click', function () {
                const modalEditarCliente = bootstrap.Modal.getInstance(document.getElementById('ModalEditarCliente'));
                modalEditarCliente.hide();
            });

            $("#InpTelefonoEdit").on("input", function () {
                $(this).val($(this).val().replace(/[^0-9+ ]/g, ""));
            });
            // Validación del formulario
            $("#FormEditarCliente").validate({
                rules: {
                    InpRazonSocialEdit: { required: true, minlength: 2 },
                    InpRFCEdit: { required: true, minlength: 12, maxlength: 13 },
                    InpPrefijoEdit: { required: true },
                    InpTelefonoEdit: { required: true, maxlength: 15 },
                   
                },
                messages: {
                    InpRazonSocialEdit: { required: "La razón social es requerida.", minlength: "Mínimo 2 caracteres." },
                    InpRFCEdit: { required: "El RFC es requerido.", minlength: "El RFC debe tener al menos 12 caracteres.", maxlength: "El RFC no puede tener más de 13 caracteres." },
                    InpPrefijoEdit: { required: "El Prefijo es requerido." },
                    InpTelefonoEdit: { required: "El Telefono es requerido.", maxlength: "El telefono no puede tener más de 15 caracteres." },
                   
                },
                submitHandler: function (form, event) {
                    event.preventDefault();
                    agregarCliente(); // Llamar a la función sin pasar IdUsuario
                },
                errorPlacement: function (error, element) {
                    error.addClass('col-12 mt-2 bg-danger text-white text-center p-2 rounded');
                    error.insertAfter(element);
                },
                errorElement: 'div',
                errorClass: 'alert-validate'
            });
            // Selecciona todos los campos del formulario de edición
            const camposEdicion = ['#InpRazonSocialEdit', '#InpRFCEdit', '#InpPrefijoEdit', '#InpTelefonoEdit', '#InpClienteImagenEdit'];

            // Función para habilitar el botón si algún campo ha cambiado
            function habilitarBotonActualizar() {
                let algunCampoCambiado = false;

                camposEdicion.forEach(function (campo) {
                    if ($(campo).val() !== '') {
                        algunCampoCambiado = true;
                    }
                });

                // Habilita el botón si algún campo ha cambiado
                if (algunCampoCambiado) {
                    $('#btnActualizarCliente').prop('disabled', false);
                } else {
                    $('#btnActualizarCliente').prop('disabled', true);
                }
            }

            // Escucha los cambios en los campos del formulario
            camposEdicion.forEach(function (campo) {
                $(campo).on('input change', function () {
                    habilitarBotonActualizar();
                });
            });

            // Escucha el evento de cambio en el input de la imagen
            $('#InpClienteImagenEdit').on('change', function (event) {
                const file = event.target.files[0]; // Obtiene el archivo seleccionado.
                if (file) {
                    const reader = new FileReader();
                    reader.onload = function (e) {
                        $('#previewImageEdit').attr('src', e.target.result); // Muestra la imagen.
                        $('#fileNameEdit').text(file.name); // Muestra el nombre del archivo.
                        $('#fileSizeEdit').text((file.size / 1024).toFixed(2) + ' KB'); // Muestra el tamaño del archivo.
                        $('#previewContainerEdit').removeClass('d-none'); // Muestra el contenedor de previsualización.
                    };
                    reader.readAsDataURL(file); // Lee el archivo como URL.
                }
                habilitarBotonActualizar();
            });

            $('#btnActualizarCliente').on('click', function () {
                const id = $(this).data('id'); // Obtiene el ID del cliente.
                const razonSocial = $('#InpRazonSocialEdit').val(); // Obtiene la razón social.
                const rfc = $('#InpRFCEdit').val(); // Obtiene el RFC.
                const prefijo = $('#InpPrefijoEdit').val(); // Obtiene el prefijo.
                const telefono = $('#InpTelefonoEdit').val(); // Obtiene el teléfono.
                const imagen = $('#InpClienteImagenEdit')[0].files[0]; // Obtiene la imagen seleccionada.
                const imagenExistente = $('#previewImageEdit').attr('src'); // Obtiene la imagen existente en base64.

                const formData = new FormData();
                formData.append('Id', id);
                formData.append('RazonSocial', razonSocial);
                formData.append('RFC', rfc);
                formData.append('Prefijo', prefijo);
                formData.append('Telefono', telefono);

                // Si no se selecciona una nueva imagen, envía la imagen existente en base64.
                if (!imagen && imagenExistente) {
                    formData.append('LogotipoBase64', imagenExistente); // Envía la imagen existente en base64.
                } else if (imagen) {
                    formData.append('LogotipoFile', imagen); // Envía la nueva imagen seleccionada.
                }

                $.ajax({
                    type: "POST",
                    url: '@Url.Action("EditarCliente", "Catalogos")', // Asegúrate de que esta ruta sea correcta.
                    data: formData,
                    processData: false,
                    contentType: false,
                    beforeSend: function () {
                        // Aquí no se oculta el grid ni se muestra el bloqueo
                    },
                    success: function (response) {
                        if (response.resultado) {
                            // Ocultar el grid de puestos
                            document.getElementById('GridClientes').style.visibility = 'hidden';

                            // Bloquear el contenido principal con el estilo "hourglass"
                            Notiflix.Block.circle('.card', 'Actualizando Cliente...', {
                                backgroundColor: 'rgba(255, 255, 255, 0.8)', // Fondo semitransparente
                                svgSize: '50px', // Tamaño del ícono de reloj de arena
                                messageFontSize: '16px', // Tamaño del mensaje
                            });

                            // Llamar a la función verPuestos para recargar el grid
                            verClientes().then((serverResponseTime) => {
                                // Tiempo adicional de 500 ms después de la respuesta del servidor
                                setTimeout(() => {
                                    // Desbloquear el contenido principal
                                    Notiflix.Block.remove('.card');

                                    // Mostrar el grid después de que termine la animación de éxito
                                    document.getElementById('GridClientes').style.visibility = 'visible';
                                }, serverResponseTime + 500); // Tiempo del servidor + 500 ms
                            }).catch(() => {
                                // En caso de error, desbloquear el contenido principal
                                Notiflix.Block.remove('.card');
                                document.getElementById('GridClientes').style.visibility = 'visible';
                                // Mostrar un mensaje de error con SweetAlert2
                                Swal.fire({
                                    title: 'Error',
                                    text: 'Hubo un error al recargar los puestos.',
                                    icon: 'error',
                                    timer: 2000,
                                    showConfirmButton: true,
                                    allowOutsideClick: false,
                                    allowEscapeKey: false,
                                    allowEnterKey: false
                                });

                                // Mostrar el grid incluso si hay un error
                                document.getElementById('GridClientes').style.visibility = 'visible';
                            });
                        } else {
                            // Mostrar un mensaje de error con SweetAlert2 si la respuesta no es exitosa
                            Swal.fire({
                                icon: "error",
                                title: "Error",
                                text: response.mensaje,
                                showConfirmButton: true,
                                allowOutsideClick: false,
                                allowEscapeKey: false,
                                allowEnterKey: false

                            });
                        }
                    },
                    error: function (jqXHR, textStatus, errorThrown) {
                        // Mostrar un mensaje de error con SweetAlert2 en caso de error en la solicitud
                        Swal.fire({
                            icon: "error",
                            title: "Error",
                            text: "Ocurrió un error al agregar el puesto.",
                            showConfirmButton: true,
                            allowOutsideClick: false,
                            allowEscapeKey: false,
                            allowEnterKey: false
                        });

                        console.error("Error en la solicitud:", textStatus, errorThrown);
                    },
                    complete: function () {
                        // Desbloquear el contenido principal después de 500 ms adicionales
                        setTimeout(() => {
                            Notiflix.Block.remove('.card');
                        }, 500);
                    }
                });

                const modalEditarCliente = bootstrap.Modal.getInstance(document.getElementById('ModalEditarCliente'));
                modalEditarCliente.hide(); // Cierra el modal.
            });

            $('#InpClienteImagenEdit').on('change', function (event) {
                const file = event.target.files[0]; // Obtiene el archivo seleccionado.
                if (file) {
                    const reader = new FileReader();
                    reader.onload = function (e) {
                        $('#previewImageEdit').attr('src', e.target.result); // Muestra la imagen.
                        $('#fileNameEdit').text(file.name); // Muestra el nombre del archivo.
                        $('#fileSizeEdit').text((file.size / 1024).toFixed(2) + ' KB'); // Muestra el tamaño del archivo.
                        $('#previewContainerEdit').removeClass('d-none'); // Muestra el contenedor de previsualización.
                    };
                    reader.readAsDataURL(file); // Lee el archivo como URL.
                }
            });

            // Evento para limpiar el formulario al cerrar el modal
            $('#ModalEditarCliente').on('hidden.bs.modal', function () {
                $('#FormEditarCliente')[0].reset(); // Restablece el formulario.
                $('#FormEditarCliente').validate().resetForm();
                $('#previewImage').attr('src', ''); // Limpia la imagen.
                $('#fileNameEdit').text(''); // Limpia el nombre del archivo.
                $('#fileSizeEdit').text(''); // Limpia el tamaño del archivo.
                
            });

            //============================================================================================================================\\
            //==========================================   Funcionalidad del botón Eliminar Cliente  ======================================\\
            //==============================================================================================================================\\
            // Función para abrir el modal de eliminación de cliente.
            function EliminarCliente(id, razonSocial) {
                $(".nombreCliente").text(razonSocial); // Muestra el nombre del cliente en el modal.
                $("#BtnEliminarCliente").data("id", id); // Asigna el ID al botón de eliminar.
                const modalEliminar = new bootstrap.Modal(document.getElementById("ModalEliminarCliente"));
                modalEliminar.show(); // Muestra el modal.
            }

            // Evento para abrir el modal de eliminación.
            $(document).on("click", ".abrirModalEliminarCliente", function () {
                let id = $(this).data("id"); // Extrae el ID del cliente usando un atributo data-*.
                let razonSocial = $(this).data("nombre"); // Obtiene la razón social.
                EliminarCliente(id, razonSocial); // Llama a la función para abrir el modal.
            });

            // Evento para cerrar el modal de eliminación.
            $(".cerrarModalEliminarCliente").on("click", function () {
                const modalEliminar = bootstrap.Modal.getInstance(document.getElementById("ModalEliminarCliente"));
                modalEliminar.hide(); // Cierra el modal.
            });

            // Evento para manejar la eliminación del cliente.
            $("#FormEliminarCliente").on("submit", function (e) {
                e.preventDefault(); // Prevenir el envío del formulario por defecto.

                const id = $("#BtnEliminarCliente").data("id"); // Obtiene el ID del cliente.
                console.log("Eliminando cliente con ID:", id); // Registra el ID en consola.

                const formData = new FormData();
                formData.append('Id', id);

                $.ajax({
                    type: "POST",
                    url: '@Url.Action("DesactivarCliente", "Catalogos")', // Asegúrate de que la ruta sea correcta.
                    data: formData,
                    contentType: false, // Importante para FormData
                    processData: false, // Importante para FormData
                    beforeSend: function () {
                        // Aquí no se oculta el grid ni se muestra el bloqueo
                    },
                    success: function (response) {
                        if (response.resultado) {
                            // Ocultar el grid de puestos
                            document.getElementById('GridClientes').style.visibility = 'hidden';

                            // Bloquear el contenido principal con el estilo "hourglass"
                            Notiflix.Block.circle('.card', 'Eliminando Cliente...', {
                                backgroundColor: 'rgba(255, 255, 255, 0.8)', // Fondo semitransparente
                                svgSize: '50px', // Tamaño del ícono de reloj de arena
                                messageFontSize: '16px', // Tamaño del mensaje
                            });

                            // Llamar a la función verPuestos para recargar el grid
                            verClientes().then((serverResponseTime) => {
                                // Tiempo adicional de 500 ms después de la respuesta del servidor
                                setTimeout(() => {
                                    // Desbloquear el contenido principal
                                    Notiflix.Block.remove('.card');

                                    // Mostrar el grid después de que termine la animación de éxito
                                    document.getElementById('GridClientes').style.visibility = 'visible';
                                }, serverResponseTime + 500); // Tiempo del servidor + 500 ms
                            }).catch(() => {
                                // En caso de error, desbloquear el contenido principal
                                Notiflix.Block.remove('.card');
                                document.getElementById('GridClientes').style.visibility = 'visible';
                                // Mostrar un mensaje de error con SweetAlert2
                                Swal.fire({
                                    title: 'Error',
                                    text: 'Hubo un error al recargar los puestos.',
                                    icon: 'error',
                                    timer: 2000,
                                    showConfirmButton: true,
                                    allowOutsideClick: false,
                                    allowEscapeKey: false,
                                    allowEnterKey: false
                                });

                                // Mostrar el grid incluso si hay un error
                                document.getElementById('GridClientes').style.visibility = 'visible';
                            });
                        } else {
                            // Mostrar un mensaje de error con SweetAlert2 si la respuesta no es exitosa
                            Swal.fire({
                                icon: "error",
                                title: "Error",
                                text: response.mensaje,
                                showConfirmButton: true,
                                allowOutsideClick: false,
                                allowEscapeKey: false,
                                allowEnterKey: false

                            });
                        }
                    },
                    error: function (jqXHR, textStatus, errorThrown) {
                        // Mostrar un mensaje de error con SweetAlert2 en caso de error en la solicitud
                        Swal.fire({
                            icon: "error",
                            title: "Error",
                            text: "Ocurrió un error al agregar el puesto.",
                            showConfirmButton: true,
                            allowOutsideClick: false,
                            allowEscapeKey: false,
                            allowEnterKey: false
                        });

                        console.error("Error en la solicitud:", textStatus, errorThrown);
                    },
                    complete: function () {
                        // Desbloquear el contenido principal después de 500 ms adicionales
                        setTimeout(() => {
                            Notiflix.Block.remove('.card');
                        }, 500);
                    }
                });

                // Cierra el modal después de iniciar la solicitud AJAX
                const modalEliminar = bootstrap.Modal.getInstance(document.getElementById('ModalEliminarCliente'));
                modalEliminar.hide();
            });

            //============================================================================================================================\\
            //==========================================  Funcionalidad del botón Reactivar Cliente  ======================================\\
            //==============================================================================================================================\\
            // Función para abrir el modal de confirmación de reactivación de cliente.
            function ConfirmarCliente(id, razonSocial) {
                $(".nombreConfirmarCliente").text(razonSocial); // Muestra la razón social del cliente en el modal.
                $("#BtnConfirmarCliente").data("id", id); // Asigna el ID del cliente al botón de confirmación.
                const modalConfirmar = new bootstrap.Modal(document.getElementById("ModalConfirmarCliente"));
                modalConfirmar.show(); // Muestra el modal de confirmación.
            }

            // Evento para abrir el modal de confirmación al hacer clic en un botón específico.
            $(document).on("click", ".abrirModalConfirmarCliente", function () {
                let id = $(this).data("id"); // Obtiene el ID del cliente desde el atributo data-id.
                let razonSocial = $(this).data("nombre"); // Obtiene la razón social del cliente desde el atributo data-nombre.
                ConfirmarCliente(id, razonSocial); // Llama a la función para abrir el modal de confirmación.
            });

            // Evento para cerrar el modal de confirmación al hacer clic en un botón de cierre.
            $(".cerrarModalConfirmarCliente").on("click", function () {
                const modalConfirmar = bootstrap.Modal.getInstance(document.getElementById("ModalConfirmarCliente"));
                modalConfirmar.hide(); // Cierra el modal de confirmación.
            });

            // Evento para manejar el envío del formulario de confirmación de reactivación de cliente.
            $("#FormConfirmarCliente").on("submit", function (e) {
                e.preventDefault(); // Evita el envío tradicional del formulario.

                const id = $("#BtnConfirmarCliente").data("id"); // Obtiene el ID del cliente desde el botón de confirmación.
                console.log("Reactivando cliente con ID:", id); // Muestra el ID del cliente en la consola para depuración.

                const formData = new FormData();
                formData.append('Id', id); // Agrega el ID del cliente al FormData.

                $.ajax({
                    type: "POST",
                    url: '@Url.Action("ReactivarCliente", "Catalogos")', // URL de la acción que reactiva al cliente.
                    data: formData,
                    contentType: false, // Importante para FormData.
                    processData: false, // Importante para FormData.
                    beforeSend: function () {
                        // Aquí no se oculta el grid ni se muestra el bloqueo
                    },
                    success: function (response) {
                        if (response.resultado) {
                            // Ocultar el grid de puestos
                            document.getElementById('GridClientes').style.visibility = 'hidden';

                            // Bloquear el contenido principal con el estilo "hourglass"
                            Notiflix.Block.circle('.card', 'Reactivando Cliente...', {
                                backgroundColor: 'rgba(255, 255, 255, 0.8)', // Fondo semitransparente
                                svgSize: '50px', // Tamaño del ícono de reloj de arena
                                messageFontSize: '16px', // Tamaño del mensaje
                            });

                            // Llamar a la función verPuestos para recargar el grid
                            verClientes().then((serverResponseTime) => {
                                // Tiempo adicional de 500 ms después de la respuesta del servidor
                                setTimeout(() => {
                                    // Desbloquear el contenido principal
                                    Notiflix.Block.remove('.card');

                                    // Mostrar el grid después de que termine la animación de éxito
                                    document.getElementById('GridClientes').style.visibility = 'visible';
                                }, serverResponseTime + 500); // Tiempo del servidor + 500 ms
                            }).catch(() => {
                                // En caso de error, desbloquear el contenido principal
                                Notiflix.Block.remove('.card');
                                document.getElementById('GridClientes').style.visibility = 'visible';
                                // Mostrar un mensaje de error con SweetAlert2
                                Swal.fire({
                                    title: 'Error',
                                    text: 'Hubo un error al recargar los puestos.',
                                    icon: 'error',
                                    timer: 2000,
                                    showConfirmButton: true,
                                    allowOutsideClick: false,
                                    allowEscapeKey: false,
                                    allowEnterKey: false
                                });

                                // Mostrar el grid incluso si hay un error
                                document.getElementById('GridClientes').style.visibility = 'visible';
                            });
                        } else {
                            // Mostrar un mensaje de error con SweetAlert2 si la respuesta no es exitosa
                            Swal.fire({
                                icon: "error",
                                title: "Error",
                                text: response.mensaje,
                                showConfirmButton: true,
                                allowOutsideClick: false,
                                allowEscapeKey: false,
                                allowEnterKey: false

                            });
                        }
                    },
                    error: function (jqXHR, textStatus, errorThrown) {
                        // Mostrar un mensaje de error con SweetAlert2 en caso de error en la solicitud
                        Swal.fire({
                            icon: "error",
                            title: "Error",
                            text: "Ocurrió un error al agregar el puesto.",
                            showConfirmButton: true,
                            allowOutsideClick: false,
                            allowEscapeKey: false,
                            allowEnterKey: false
                        });

                        console.error("Error en la solicitud:", textStatus, errorThrown);
                    },
                    complete: function () {
                        // Desbloquear el contenido principal después de 500 ms adicionales
                        setTimeout(() => {
                            Notiflix.Block.remove('.card');
                        }, 500);
                    }
                });

                // Cierra el modal de confirmación después de iniciar la solicitud AJAX.
                const modalConfirmar = bootstrap.Modal.getInstance(document.getElementById('ModalConfirmarCliente'));
                modalConfirmar.hide();
            });

        });

    </script>
}